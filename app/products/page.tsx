import styles from './styles/Products.module.css';

const ProductsPage = () => {
    return (
        <section id={styles.products}>
            <h1>products</h1>
        </section>
    );
};

export default ProductsPage;
